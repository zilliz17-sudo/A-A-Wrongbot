# Copy to ".env.local" in this folder (Next.js loads it; that file is gitignored).
#
# In .env.local only — never commit real values — set:
#   - the server env var name used by app/api/reframe/route.ts (see README)
#   - optional model override documented in README
#
# Keys are issued from: https://console.groq.com
